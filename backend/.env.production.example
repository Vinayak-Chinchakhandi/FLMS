# Railway sets these automatically — do NOT commit this file
# PORT is set by Railway
# NODE_ENV=production

# Paste the Railway PostgreSQL connection string here:
DATABASE_URL=postgresql://postgres:password@containers-us-west.railway.app:PORT/railway

# Open CORS for mobile app + any frontend deployment
ALLOWED_ORIGINS=*
